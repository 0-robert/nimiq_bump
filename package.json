{
  "name": "bump",
  "version": "1.0.0",
  "description": "One slot. Replace it by paying 1.25x, and the previous poster gets the money.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.js test/*.test.ts",
    "copycheck": "node scripts/copycheck.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.132.0"
  }
}
